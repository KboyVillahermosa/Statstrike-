# Community.vue Update Task

## Completed Tasks
- [x] Replace the entire <script setup> section with the new code provided (includes FormData for image upload, formattedPosts computed property).
- [x] Update the template's v-for directive from 'post in posts' to 'post in formattedPosts'.
- [x] Update the image display div to use 'post.image_url' instead of 'post.image'.

## Followup Steps
- [ ] Test the image upload functionality after changes.
- [ ] Ensure backend is configured as per the instructions (storage link, Post model accessor, controller updates).
